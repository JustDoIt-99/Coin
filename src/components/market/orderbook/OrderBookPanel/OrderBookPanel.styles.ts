import styled from "@emotion/styled";

export const Panel = styled.div`
    position: relative;
    display: grid;
    width: 350px;
    background: #fff;
    overflow-y: auto;
    overflow-x: hidden;
`;

export const OrderBookList = styled.div`
    display: flex;
    flex-direction: column;
    overflow-y: auto;
`;